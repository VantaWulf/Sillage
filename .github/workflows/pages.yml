# Disabled: using GitHub Pages "Deploy from branch: gh-pages" only.
# Dual deploys (Actions + branch) were racing and delaying the public site.
name: Deploy GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Settings → Pages → branch gh-pages instead."
